name: Dot Net API Build and Test

on:
  pull_request:
    paths:
      - '**/*.cs'
      - '**/*.csproj'
      - '.github/workflows/**'
  push:
    branches:
      - main
      - develop
    paths:
      - '**/*.cs'
      - '**/*.csproj'
      - '.github/workflows/**'
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  security-events: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'
      - name: Restore
        run: |
          for proj in $(find . -name '*.csproj'); do
            dotnet restore "$proj"
          done
      - name: Install formatter
        run: dotnet tool update -g dotnet-format
      - name: Lint
        run: |
          export PATH="$HOME/.dotnet/tools:$PATH"
          for proj in $(find . -name '*.csproj'); do
            dotnet format "$proj" --verbosity diagnostic
            dotnet format "$proj" --verify-no-changes --no-restore
          done
      - name: Build
        run: |
          for proj in $(find . -name '*.csproj'); do
            dotnet build "$proj" --configuration Release --no-restore
          done
      - name: Test
        run: |
          for proj in $(find ./5-Test -name '*.csproj'); do
            dotnet test "$proj" --no-build --configuration Release
          done

  codeql:
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: csharp

      - name: Autobuild
        uses: github/codeql-action/autobuild@v4

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4

  secret-scanning:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          # For pull requests, we need the merge commit and base branch
          # For push events, we only need the current commit
          fetch-depth: 0

      - name: Run TruffleHog (Pull Request)
        if: github.event_name == 'pull_request'
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          base: ${{ github.event.pull_request.base.sha }}
          head: ${{ github.event.pull_request.head.sha }}
          extra_args: --only-verified --fail --no-update

      - name: Run TruffleHog (Push/Workflow Dispatch)
        if: github.event_name != 'pull_request'
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          extra_args: --only-verified --fail --no-update

  dependabot:
    runs-on: ubuntu-latest
    if: github.actor == 'dependabot[bot]'
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Check for outdated packages
        run: |
          for proj in $(find . -name '*.csproj'); do
            echo "Checking $proj for outdated packages..."
            dotnet list "$proj" package --outdated || true
          done
